<h2>✏️ עריכת ספר</h2>

<form action="/books/<%= book._id %>?_method=PUT" method="POST" class="book-form">
    <div class="form-group">
        <label for="title">כותר:</label>
        <input type="text" id="title" name="title" value="<%= book.title %>" required>
    </div>

    <div class="form-group">
        <label for="author">מחבר:</label>
        <input type="text" id="author" name="author" value="<%= book.author %>" required>
    </div>

    <div class="form-group">
        <label for="price">מחיר:</label>
        <input type="number" id="price" name="price" step="0.01" value="<%= book.price %>" required>
    </div>

    <div class="form-actions">
        <button type="submit" class="btn edit-btn">💾 שמור שינויים</button>
        <a href="/" class="btn cancel-btn">🚫 ביטול</a>
    </div>
</form>
